import CountryList from './CountryList';
import './App.css';

function App() {
  return (
    <div className="App bg-gradient-to-r from-blue-100 to-cyan-100 w-screen h-screen">
      <header className="App-header">
        <CountryList/>
      </header>
    </div>
  );
}

export default App;
